generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model usuario {
  id    Int    @id @default(autoincrement())
  nome  String
  senha String
  email String @unique
  idade Int
}
